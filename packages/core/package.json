{
  "name": "core",
  "version": "0.0.0",
  "private": true,
  "description": "Core logic for Ordinex",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/uuid": "^11.0.0",
    "typescript": "^5.3.3",
    "vitest": "^1.0.4"
  },
  "scripts": {
    "build": "tsc -p ./",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "uuid": "^13.0.0",
    "@anthropic-ai/sdk": "^0.32.1"
  }
}
